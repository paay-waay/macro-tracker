# 3.0.0 验证记录

验证日期：2026-09-23。

## 自动化数据检查

18 项数据兼容测试通过：原始对象不变、更近草稿恢复、旧草稿不覆盖、草稿独立日期、原评分保留、v8 本地存储、IndexedDB 优先级、零与未知、配方缩放、CSV 中文多行引号、无效文件拒绝、CSV/JSON 往返、合并冲突、完整日统计分母、历史目标。

实际 2.3.5 CSV 验证读入：112 个日期、475 条食物、25 个常吃。只用于本机验证，个人 CSV 不随发布文件夹提供。

## 浏览器真实存储检查

以独立本机来源写入合成 2.3.7 记录、草稿、常吃和目标后升级：

- 较新草稿恢复，目标和常吃出现。
- 一键添加、撤销、整餐记录和未知蛋白正常。
- 刷新后新记录仍在。
- CSV 导入预览与确认、半份缩放正常。
- 500 g、600 kcal、蛋白 90 g 的称重配方记录 200 g 后为 240 kcal、蛋白 36 g；常吃入口记住 200 g。
- 旧 records、drafts 原值保持不变；原始快照存在。
- 重复启动不重复迁移；旧修订号的并发写入被拒绝。
- IndexedDB 事务提交后重新打开仍可读取。
- 周回顾不会把未知蛋白计入可判断日。
- 首次联网安装离线缓存后，停止本机服务器并重新载入页面，应用与记录仍可读取。

## 范围

已检查 JavaScript 语法及 GitHub Pages 子路径资源。测试使用本机浏览器与合成数据库，不直接操作现有线上个人数据库。浏览器同源隔离是数据迁移前提，换设备或域名需导入备份。未替用户部署线上网站。
