# 3.1.0 验证

- 18 项数据兼容检查通过；实际旧版 CSV 的 112 天、475 条食物、25 个常吃可完整解析。
- 浏览器 IndexedDB 检查通过：旧记录、草稿原值未修改；原始备份存在；重复打开不重复迁移；过期页面的写入被拒绝；写入后重开仍在。
- 合成 3.0 状态的 7 天记录、3 个常吃，在 3.1 打开后逐项相同。
- UI 实测：四项宏量一次保存、小数保留、常吃一键添加、撤销、旧配方以已有宏量复用、编辑旧常吃后新记录采用新值。
- 周回顾示例中 5 个完整日均值为 2,202 kcal；2 个部分记录日未进入均值。无完整目标时不显示目标线。
- 320 与 390 CSS 像素视口没有横向溢出。首次联网后停止本机服务器，重新载入仍可读取页面和记录。
- 已检查 JavaScript 语法。未在实体 iPhone 上验证手势缩放、软键盘和主屏幕安装行为；需在设备上最终确认。
- 验证使用本机模拟数据。个人 CSV 与浏览器数据库不随交付文件夹提供。
